{"file": ".trellis/spec/frontend/component-guidelines.md", "reason": "Instrument Scan Panel contract (CoinScanPanel.tsx owns parameter area + results table) plus the rules this change must respect: keep pure math in testable helpers instead of inline JSX, and keep the results component's top-level element count stable (header + table) so .workspace grid rows are not stolen. Also the .coin-scan-* styling/命名 convention."}
{"file": ".trellis/spec/frontend/quality-guidelines.md", "reason": "UI change verification rules: .test.tsx must import @testing-library/jest-dom/vitest for matchers, prefer stable structural queries (class/role/aria) over mojibake Chinese literals, and run npm test after changes. Explains why the new tests query classes rather than button text."}
{"file": ".trellis/spec/frontend/state-management.md", "reason": "App.tsx owns page-level React state and there is no global store; persistence conventions for browser-side state. Confirms the demote marker belongs in App state + localStorage, mirroring leaderCoins, with no server route."}
{"file": ".trellis/spec/server/coin-scan.md", "reason": "Defines ScanRow/ScanResponse, the row sorting contract (qualifiedCount desc → bestScore desc) and the convergence fields the results table renders. Confirms the demote feature must not alter server sorting or any response field."}
